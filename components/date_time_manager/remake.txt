date_time_manager_set_mode -> refazer funcao para que o modo auto busque timezones de forma automatica por meio da reimplementacao da funcao sntp_time_sync_notification_cb.
Alem disso, avaliar novas configuracoes para a funcao sntp da lib esp_sntp.h. Por exemplo, server utilizado, intervalo de tempo para sincronizacao.

