Definir configuracoes iniciais para utilizar o driver
    -quantidade de botoes a ser instalados
    -tempo de debounce (Evitar falsas interrupcoes)
    -tempo de polling  (Monitoramento continuo do nivel logico do botao)
    -tempo de press long (tempo pressionado para considerar acao de click longo)
    -tempo de autorepeat (tempo pressionado para considerar habilitar acao de autoclick)
    -tempo de intervalo autorepeat (tempo pressionado para considerar acao de click)

Usuario cria o descritor de um botao e, passa como parametro para instalacao.
    -GPIO acoplado ao botao; num_GPIO 
    -utiliza resistores internos; True or false
    -nivel logico do botao quando pressionado; high or low
    -utiliza funcao especial autorepeat para click; True or false
    -utiliza funcao especial pressed long para click longo; True or false
    -numero do pino de qual sera utilizada a fila de maneira compartilhada. -1 para exclusivo or num_GPIO para compartilhada

implementacao das funcoes

install_button
    se-> existe espaco para instalar botoes
        1-Inicia configuracoes do gpio, baseado no descritor do button
        se-> primeiro botao a ser instalado?
            1-seta configuracoes iniciais do descritor
            2-Instala o driver de servico de interrupcao
        senao->
            1-Valida se o pino a ser instalado, nao é um pino repetido;
            2-procura se existe fila do pino passado para usar de forma compartilhada

        se-> se pino de fila compartilhada igual -1, caso de fila exclusiva
            Cria nova fila
        entao se-> fila compartilhada foi encontrada
            Seta ref. nas config internas do descritor
        senao->
            Erro de instalacao, fila do pino passado como argumento nao foi encontrada

        2-Cria argumentos do software timer e cria o timer
        3-Configura todos os parametros no descritor do botao
        4-ativa interrupacao no pino do botao
    senao->
        Erro, sem espaco para instalacao

uninstall_button
get_queue